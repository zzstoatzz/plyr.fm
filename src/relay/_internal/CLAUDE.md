# _internal

internal services not exposed as public APIs.

- auth: session management, OAuth state encryption
- uploads: multipart handling, format validation
- queue: shuffle/repeat logic, state persistence
- notifications: user preferences, delivery (future)

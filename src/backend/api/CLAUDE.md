# api

public HTTP endpoints for relay.

- all endpoints except `/auth/*` require session authentication
- OAuth 2.1 flow: authorize, callback, logout
- main resources: tracks, artists, audio streaming, queue, preferences

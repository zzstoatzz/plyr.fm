set shell := ["bash", "-eu", "-o", "pipefail", "-c"]
default := "run"

alias r := run
alias dev := run

# run frontend dev server (hot reloads)
run:
    bun run dev --host 0.0.0.0

# check svelte types
check:
    bun run check

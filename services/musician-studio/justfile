check:
    uv run ruff check .
    uv run ruff format --check .
    uv run python -m pytest tests -q

image:
    docker build -t plyr-musician-python:local .

compose musician:
    uv run python compose.py {{musician}}

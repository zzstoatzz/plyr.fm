[project]
name = "plyr-musician-studio"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = ["pydantic>=2,<3", "httpx>=0.27,<1", "numpy>=2,<3", "prefect==3.8.2"]

[dependency-groups]
dev = ["pytest>=8", "ruff"]
